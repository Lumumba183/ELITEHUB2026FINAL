[build]
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "18"

# SPA redirect — ensures React Router client-side routes work on Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
